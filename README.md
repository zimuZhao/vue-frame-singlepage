A building framework for Vue. To build a single-page application

```
npm install
npm run dev
npm run build
```
when use `npm run build`, u should remember modified the 'entry' from 'webpack.prod.conf.js'
